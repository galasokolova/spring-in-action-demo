version: '4.28.0'
services:
  rabbitmq:
    image: rabbitmq
    hostname: rabbit-name
    ports:
      - "15672:15672" # Опционально: для доступа к управляющей панели RabbitMQ

  chap_9_rabbitmq:
    build: ./
    depends_on:
      - rabbitmq
    ports:
      - "8080:8080" # Проброс порта для chap_9_rabbitmq
